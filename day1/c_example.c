#include <stdio.h>
int main()
{

    int ch;


    while ((ch = getchar()) != EOF) {
        putchar(ch);
    }

    a = get_value();
    if (a = 10) {
        printf("Got some value...\n");
    }
    
    return 0;
}

